# Placeholder values for Vitest (mode "test"), where no .env.local exists —
# importing @/lib/env must not throw. Tests never hit these URLs.
VITE_CONVEX_URL=https://test.convex.cloud
VITE_CONVEX_SITE_URL=https://test.convex.site
