# Copy to .env.local.

# The Vita OS API Worker (apps/api). It serves Better Auth's browser routes at
# /api/auth/* and the application operations at /v1/*. Locally this is whatever
# `bunx wrangler dev` prints for apps/api.
VITE_API_BASE_URL=http://localhost:8787
