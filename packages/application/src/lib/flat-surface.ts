/** Dividers-only list on the page surface — no inset panel. */
export const flatListClassName = "divide-y divide-border/50";

/** Row hover inside flat lists. */
export const flatListRowHoverClassName = "transition-colors hover:bg-muted/50";

/** Section block without inset surface box. */
export const flatSectionClassName = "space-y-3";
